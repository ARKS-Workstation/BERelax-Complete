# Open Questions

Everything the build is carrying as an assumption or a stub, with who resolves it and what it blocks.

**The rule** ([12-autonomous-delivery.md](12-autonomous-delivery.md) §1): nothing here stalls the build.
Each item has a **provisional value chosen as the strictest safe option**, is flagged `provisional: true`
in settings, appears in the **Unconfirmed Assumptions panel**, and is corrected with one audited settings
change at deploy-and-check time.

Statuses: `open` · `asked` (with whom and when) · `resolved` (with the answer and date).

---

## A. Business rules — needed before the units that use them

| ID | Question | Provisional value | Status | Blocks |
|---|---|---|---|---|
| Y9-gender | Must therapists serve only clients of the same gender? Are gender-zoned rooms required? | **Strict same-gender matching, enforced** | open | `B-AVAIL` |
| Y9-turnaround | Real turnaround per service type. The **wet room** almost certainly needs longer than a standard room | **20 min standard, 30 min wet room** | open | `B-CAT`, `B-AVAIL` |
| Y9-windows | Cancellation window, late-cancellation and no-show policy | **24h window; no fee charged, flagged only** | open | `B-LIFE` |
| Y9-buffer | Therapist buffer before/after a treatment, distinct from room turnaround | **10 min each side** | open | `B-AVAIL` |
| Y9-lead | Minimum online booking lead time and maximum advance window | **2h lead, 90 days ahead** | open | `B-AVAIL` |
| Y9-commission | Therapist commission structure — flat, tiered, service-dependent | **None configured; commission module ships disabled** | open | `P-HR` |
| Y9-package-policy | Package validity, transferability, and unredeemed-balance treatment at expiry | **6 months, non-transferable, balance retained not forfeited** | open | `M-TILL` |
| Y9-package-thin | Policy for a pre-system package with a thin paper trail — honour, honour-once-on-evidence, or decline | **Honour once on evidence, logged** | open | `H-MIG` |

## A2. Opened by the unit decomposition

Each of these is a value a build unit needs and that no document supplies. They were found by
decomposing the plan into machine-checkable acceptance criteria — the point at which "to be confirmed"
stops being acceptable. Every one has a provisional value chosen as the strictest safe option, and each
is flagged `provisional: true` in settings so it appears in the Unconfirmed Assumptions panel.

| ID | Question | Provisional value | Status | Blocks |
|---|---|---|---|---|
| Y9-buffer | Therapist buffer before/after a treatment, as distinct from room turnaround | **10 min each side** | open | availability engine |
| Y9-overtime | Overtime uplift percentages, the night window, the daily and weekly caps, the day a working week starts on, the minimum rest between shifts, and what a public holiday worked is paid at, under MOHRE rules | **Every figure is the build's strictest reading of Federal Decree-Law 33 of 2021 and NONE is confirmed: 8h/day, 48h/week starting Monday, 1.25x overtime capped at 2h/day, 1.5x for minutes between 22:00 and 04:00, 1.5x on a public holiday worked, and 11h minimum rest between presences.** They are VERSIONED rows in `working_hours_rule` (migration 0059, P-HR-05) rather than `app_setting` values, and that is the decision the answer has to live with: payroll is asked about the PAST, so recomputing March after an April rate change must use March's rates, and one current value cannot say what they were. Answering this therefore publishes a NEW version and the Unconfirmed Assumptions panel row leaves by that version being confirmed — the same shape `consent_wording` takes. Two of the figures have no number in the handover at all. The **public-holiday multiplier** reuses the night window's 150% because the alternative the law offers, a compensatory rest day, cannot be expressed as a multiplier, so anything dearer would be a guess dressed as the strict reading. The **week boundary** is the one whose wrongness is silent: it moves minutes between weeks without changing any day's total, so the weekly cap is computed over the wrong seven days and nothing looks odd. Separately, the holiday CALENDAR is not this build's: `premises_closure` carries `kind = public_holiday` and a public holiday the premises trades through has no row at all, so the derived set is a floor and the holiday flag reaches the maths as an argument | open | rota validator, payroll, `P-HR-05` |
| Y9-coverage | Minimum floor coverage, the wet-room minimum, the segment grid the floor is counted on, and the daily treatment-load caps per therapist | **2 therapists on the floor in every 30-minute segment; at least 1 of them able to deliver a wet-room treatment whenever the wet room is bookable; max 6 treatment-hours/day/therapist, max 4 of them high-intensity. NONE is confirmed.** They are VERSIONED rows in `rota_coverage_rule` (migration 0081, P-HR-06) rather than `app_setting` values, for the reason Y9-overtime records and a sharper version of it: a rota is asked about the PAST — "was the floor covered on the 4th of March?" is a question about a rota published months ago — so raising the floor minimum in April must not make March's rota retroactively non-compliant, and one current value cannot say what the threshold was. Sharper, because a published `rota_version` is IMMUTABLE and names the rule version that judged it, so the record is "this rota satisfied THESE thresholds" rather than "this rota was valid" — and only the first stays true. Answering this therefore publishes a NEW version and the Unconfirmed Assumptions panel row leaves by that version being confirmed, the same shape `consent_wording` takes. **Three parts of the answer are unsettled and marked rather than smoothed over.** (1) The DEEP-TISSUE sub-cap has nothing to measure: the catalogue contains no deep-tissue treatment, because 0004's regulatory profile refuses "Therapeutic Deep Tissue" as a CLAIM on a service name, so nothing records which of the twelve services is heavy work. `high_intensity_treatment_codes` is seeded EMPTY and the 240-minute cap is INERT until this is answered — deliberately not the strictest reading, because "every treatment is heavy work" turns the sub-cap into the real daily cap and refuses rotas nobody was asked about, while a list of three invented ones is brief rule 15. The rule is fully implemented and tested; answering it is one row. **What is needed: which treatments count as high-intensity.** (2) WET-ROOM CAPABILITY is DERIVED rather than recorded: `therapist_skill` is `(asian_style, arabic_style)`, 0012 maps `morocco_bath_jacuzzi` to the wet room for both styles, and ADR 0021 makes style an attribute of the TREATMENT — so "can run the bath" is currently "holds the skill a wet-room service requires", which every skilled therapist does. A `wet_room` skill value would invent a training fact about nineteen people (Y12-names). **What is needed: which therapists are trained on the Moroccan bath, if it is not all of them.** (3) The 30-minute SEGMENT GRID is this build's, matching the booking slot grid, and it is a judgement about how short a gap in cover may be. A therapist counts towards a segment only if present for the WHOLE of it, which is the strict reading. | open | leave approval, rota validator, `P-HR-06`, `P-HR-07` |
| Y9-leave-detail | Probation length, first-year and pro-rata accrual, carry-over cap and expiry, the leave-year anchor, the sick-leave band lengths, which absences stop accrual, and whether a public holiday inside annual leave is counted | **6-month probation with accrual running from day 1 and no leave taken until it ends; 30 calendar days a year at 2.5 a month, a part month pro-rated on the days employed; a 30-day carry-over cap; sick leave 15 full / 30 half / 45 unpaid days per illness; unpaid and ABSENT days not earning accrual.** They are VERSIONED rows in `leave_entitlement_rule` (migration 0066, P-HR-08) rather than `app_setting` values, for the reason Y9-overtime records: leave is asked about the PAST, so a disputed month recomputed after a policy change must use the policy that applied then. Answering this publishes a NEW version and the Unconfirmed Assumptions panel row leaves by that version being confirmed. **Three parts of the answer are unsettled and are marked as such rather than smoothed over.** (1) CARRY-OVER EXPIRY: this row previously said carried days expire after 12 months and P-HR-08's manifest entry said they do not. 0066 seeds them as NOT expiring, because expiring entitlement that should not expire removes days invisibly while keeping days that should have lapsed only makes a balance too high — of two errors, take the visible one. The engine implements both, so the other answer is a one-row change. (2) The LEAVE YEAR runs from the employment anniversary, derived from `employee.employed_from`, because any fixed calendar anchor other than 1 January would be a date the build invented (brief rule 15). (3) A PUBLIC HOLIDAY INSIDE LEAVE is **counted** as a leave day, which is the opposite of the position this row used to record: a leave day is a calendar day, and `premises_closure` is a floor rather than a holiday calendar — a public holiday the premises trades through has no row at all, so excluding holidays would exclude an unknown and varying subset of them. Deferred to P-HR-10, which builds the calendar | open | leave accrual engine, `P-HR-08`, `P-HR-09`, `P-HR-10` |
| Y9-frequency-cap | **How often may this business message one contact?** docs/03 §5 wants drag-and-drop journeys and names no frequency, and the figure is not a preference: three unrelated journeys — a win-back sequence, a birthday greeting and a February campaign — each sending "only one message" collectively spam one person, every one of them inside its own rule, and docs/04 §5 says TDRA's sanction is sender-ID SUSPENSION rather than a per-message fine, so the penalty falls on the identity the booking confirmations also leave from | **2 per rolling 7 days and 6 per rolling 30 days, counted per CONTACT across every flow, every campaign and every channel.** Held as `messaging.frequency_cap_per_week` and `messaging.frequency_cap_per_month` in `app_setting`, both `is_provisional` with this id, so they appear in the Unconfirmed Assumptions panel and leave it by being confirmed — answering this is ONE audited settings change per figure, with no migration and no code change. The windows are ROLLING and not calendar periods: a calendar week resets at midnight, so a contact messaged twice on Sunday evening could be messaged twice more on Monday morning, four messages in twelve hours and every one inside a cap of two. A month is not a duration, so "per month" is pinned to 30 days — the shorter of the two obvious readings, therefore the stricter. Each refusal records `bound_cap_limit` and `bound_cap_window_seconds` in `frequency_ledger`, so "why was this refused in March" is answered with March's figure rather than April's. What the answer CANNOT be is "no cap": 0, null and 'unlimited' are refused by `assertFrequencyCapLimit` (`@berelax/core`), by the settings schema and by `frequency_cap_value_is_a_cap()` in migration 0080, so the refusal holds for a `psql` session too. Zero is refused although it looks strictest, because in every other `max_` setting zero ALSO means "no limit" and a reader that treats it as falsy turns the strictest value into the switched-off one; stopping promotional traffic altogether is the marketing kill switch's job (`C-AUTO-05`), which says so on its face and records who engaged it | open | `C-AUTO-03`, `C-AUTO-07`, `C-AUTO-10`, every promotional send |
| Y9-ramadan-window | Whether the 07:00–21:00 promotional SMS window narrows during Ramadan | **Narrowed to 10:00–16:00 during Ramadan** (the conservative guess; a wider window would risk a breach) | open | campaign scheduler |
| Y9-queued-staleness | What happens to a time-sensitive promotional message queued at 23:00 whose offer expires before the window reopens | **Expires unsent, with a report to the owner rather than a late send** | open | campaign scheduler |
| Y9-tips | Tip model: cash vs card, pooled vs individual, and the payout and tax treatment | **Cash only, individual, recorded but not banked — no card tips until the model is decided** | open | till, payroll, payments |
| Y9-deposits | Which services require a deposit, what percentage, and whether first-time clients prepay | **Deposits DISABLED. No service requires one** | open | payments |
| Y1-analytics-credentials | GA4 measurement id and API secret, Meta dataset id and CAPI access token, and Meta Business verification | **Fake analytics provider; no real ids configured** | open | real analytics dispatch |
| Y11-vat201-boxes | The actual VAT201 box numbers for standard-rated sales, reverse charge and recoverable input VAT | **Box 1 / Box 3 / Box 10 as placeholders, held in a data table with a test proving the mapping is data not code** | open | VAT return working papers |
| Y11-rounding | Confirm the gross-to-net rounding convention is acceptable to the tax agent | **half-up on net, VAT as the remainder** (exactness proven either way) | open | first VAT return |
| Y9-cooling-off | How long after a review is left may a reply be auto-sent? docs/07 SS4 says "after a cooling-off delay" and names no number, and the delay is the only thing standing between an auto-sent reply and a review the reviewer is still editing | **24 hours, enforced as a FLOOR rather than a figure: the setting can be lengthened and cannot be shortened, and auto-send is off by default anyway** | open | review autoresponder |
| Y9-crm-lifecycle | What are the client lifecycle stages, and what counts as lapsed? Nothing in the handover describes a client lifecycle, and C-CRM-01 needs one before a win-back flow or a Kanban pipeline can exist | **Six states — `lead`, `new`, `active`, `lapsing`, `lapsed`, `blocked` — held in `customer_lifecycle_state` (migration 0053) as a TABLE rather than a Postgres enum, so every label carries `is_provisional` and this id and appears in the Unconfirmed Assumptions panel.** The two inactivity thresholds that move a record are events supplied by the caller, not durations this build has chosen, and `blocked` deliberately does not lapse — a block that expired on a timer would be an unblock nobody decided | open | `C-CRM-01`, `C-AUTO-08`, the win-back flow |
| Y9-crm-source | Which acquisition channels does the business count, and how is a walk-in attributed? | **Six sources — `walk_in`, `whatsapp`, `phone`, `web`, `referral`, `unknown` — taken from the channels docs/13 shows in use, in `customer_acquisition_source` (0053) with the same provisional marking. `unknown` is the DEFAULT**, because it is true of every record the business already has: choosing `walk_in` for them would be an invented marketing attribution, and a plausible attribution is indistinguishable from a recorded one | open | `C-CRM-01`, any report of where clients come from |
| Y9-crm-pipeline | **What are the stages of this business's sales pipeline?** docs/03 §5 asks for lead and client cards dragged between ordered stages and names none of the stages, and a column is not a presentation choice: a stage is a claim the front desk makes about a person, so the wrong six columns are six wrong claims made every day | **Six stages — `new_enquiry`, `contacted`, `booked`, `attended`, `repeat`, `lapsed` — held in `pipeline_stage` (migration 0077) as a TABLE rather than a Postgres enum, so every label carries `is_provisional`, this id and a note, and appears in the Unconfirmed Assumptions panel.** Deliberately NOT the same vocabulary as `customer_lifecycle_state` (Y9-crm-lifecycle): the lifecycle is derived from what has happened and a pipeline stage is where a human has PUT somebody, and the two disagree on purpose — a record can be `active` in the lifecycle and `lapsed` on the board because the front desk has given up on them. A single column could only answer one of the two questions. Answering this is a reorder and an archive rather than a migration, because the stages are rows: `reorderPipelineStages` takes the whole order as a permutation and an unwanted column is archived with its position kept, since a transition row names the stage it moved a card into and that log cannot be rewritten | open | `C-AUTO-08`, `C-AUTO-09`, any report of where enquiries are lost |
| Y9-consent-purpose | What does this business ask consent FOR? docs/04 §8 requires consent per channel and per purpose and names no purposes, and a consent record is only a defence if the purpose it was given for is the purpose the send is made under | **Four purposes — `marketing`, `review_request`, `clinical_processing`, `photography` — in `consent_purpose` (migration 0056) as a TABLE rather than a Postgres enum, so every label carries `is_provisional`, this id and a note, and appears in the Unconfirmed Assumptions panel.** `is_send_gating` is true for the first two only: a photography or clinical-processing grant is the lawful basis for holding a record, not permission to message anybody, and a gate built on one would allow every send while looking correct. **Transactional service updates are deliberately absent** — they are not consent-gated, and a purpose somebody could later gate a booking confirmation on is how a consent outage becomes an operational one | open | `C-CRM-03`, `C-CRM-04`, every promotional send |
| Y9-consent-wording | **What exactly does the customer read when they opt in?** docs/04 §8 requires the exact wording shown to be stored, versioned and hashed in EN and AR. That wording is legal copy and the build has seen none | **A drafted version 1 per purpose, seeded by `packages/db/src/seed/consent.ts`, whose text OPENS with `[DRAFT WORDING — not approved copy]` in both languages** and which carries `is_provisional`, this id and a note. The marker is in the text itself and not only in a flag, because rule 15's point is that plausible legal copy in a table is indistinguishable from approved legal copy: a reader of the row, and a fixture customer shown it, must both be able to see it is a draft. It deliberately does NOT use a marker `is_placeholder_text()` refuses — a wording the database would not store makes every consent path untestable, which is worse. `consent_wording` is append-only, so answering this publishes version 2 and version 1 stays as the record of what earlier consents were given under | open | `C-CRM-03`, the first real promotional send, the preference centre (`C-CRM-04`) |
| Y9-dnc-register | **Is the national do-not-call register binding on this business directly, or only on the aggregator that carries its SMS?** The two readings differ in what happens when a number on the register is on a campaign list: under one the aggregator drops the message, under the other sending it at all is the breach. Nothing in the handover says which, and the sanction docs/04 §5 describes is sender-ID suspension rather than a per-message fine, so the cost of being wrong is every campaign this business can run | **Treated as binding on US as well — the stricter reading.** `dnc_register` is a `suppression_source` in migration 0064, and a key suppressed under it refuses a promotional send at `evaluateGate` before the aggregator ever sees the message. If the looser reading turns out to be correct nothing has to change: the entries stay and the business has been stricter than required. If the stricter one is correct and this had been built the other way, the first campaign would be the breach | open | `C-CRM-04`, the first real promotional campaign, whoever owns the aggregator contract |
| Y9-dedup-thresholds | At what confidence may two client records be merged without a human looking, and at what confidence should a human be shown the pair at all? Nothing in the handover says, and the two numbers are the whole of the policy: they decide how often the front desk is interrupted and how often two people are joined into one record by mistake | **Auto-merge at 0.95, human review at 0.70, deliberately conservative — a false merge is far more expensive than a missed one.** Held as named constants in `packages/core/src/crm/duplicate-score.ts` rather than as settings rows, because nothing reads a setting for them yet and a knob no code consults lies about what it controls; `duplicateVerdict` takes the pair as an argument so the unit that acts on them (C-CRM-05) can inject the owner's figures without this module changing. The auto band is reachable **only** with an identical phone number, so no label agreement can merge two different numbers unattended | open | `C-CRM-02`, `C-CRM-05`, the duplicates review screen |
| Y1-llm-pricing | The per-million-token rate the chosen LLM provider actually charges, in fils. Needed because the per-run agent budget is an integer-fils cap (ADR 0007) and a cap measured in the wrong unit is not a cap: an under-reported run spends a month's budget in an afternoon and the bill is unexplainable by the runs that caused it | **Provisional rates in `packages/providers/src/llm/named-fakes.ts`, marked as such, deliberately NOT copied from either vendor's pricing page** — a figure copied there would be indistinguishable from a configured one the moment it went stale. They exist only to make the cap testable and the two providers distinguishable; MiniMax is the dearer of the two so that switching provider visibly changes the recorded cost | open | the first real LLM call, and any report of spend against the monthly token budget |

---

## B. Legal and regulatory

| ID | Question | Provisional value | Status | Blocks |
|---|---|---|---|---|
| Y1-licence | Licence classification: commercial wellness or a healthcare activity? Permitted public vocabulary and staff titles follow from it | **`licence_class: unconfirmed` → resolves to the stricter combination: wellness vocabulary, healthcare-grade retention.** C-CRM-08 makes both halves enforced rather than documented for clinical intake: `clinical.intake_copy_lints_questions` (provisional, default **true**) puts every intake QUESTION label, help text and choice through the publication lexicon as well as the form's title and consent paragraph, and `clinical.intake_submission.retain_until` is computed at capture from `regulatory_profile_current.clinical_retention_years` — the healthcare-grade 25 years — and stored, so a past decision stays explainable after the profile changes. **What answering it the other way buys, in both directions:** `wellness` turns the question lint off (only the title and the consent wording are checked); `healthcare` additionally flips `medical_claims_permitted`, which stops the profile's claim list applying to the title and the consent wording too, and lowers the retention figure. Both are configuration changes on the Unconfirmed Assumptions panel, not code changes. The one rule a question is NEVER linted for is the claim list itself — asking about medication is not claiming to prescribe it — and that exemption is one rule wide and named in `packages/core/src/clinical/intake.ts`. **C-CRM-09 makes the vocabulary decide a COLUMN NAME as well as copy, and that half is not a configuration change.** The eighth contraindication flag is `requires_consultation` and not `practitioner_review_required`, because `practitioner` is a `PROVIDER_TITLES` entry the seeded `permitted_public_titles` does not permit — so the label is refused by `unpermitted_staff_title`, asserted with a control in `packages/core/src/clinical/contraindication-flags.test.ts`, and every flag label and action line on the front-desk screen goes through the full publication lexicon (`lintContraindicationCopy`). **What answering `healthcare` buys here:** a wider label vocabulary, so a marker may name who reviews it. It does NOT rename the column — a database column is not copy, and a synonym migration is not a licence classification | open | public copy in `W-SITE`, intake template wording (`C-CRM-08`), contraindication marker copy (`C-CRM-09`) |
| Y5-residency | Do intake notes count as health data subject to UAE localisation? Decides hosting region | **Clinical boundary built and isolated; no real intake data loaded — and from C-CRM-08 that is ENFORCED rather than a convention.** `clinical.intake_submission.data_origin` is `synthetic` or `real`, required with no default, and a `real` payload is refused by name (`ClinicalRealIntakeNotPermitted`, and `ZJ005` in migration 0082) while the setting `clinical.real_intake_permitted` is false — which it is by default, and an ABSENT setting row reads as false too, because a gate whose default is "permitted" when its configuration is missing is a gate that opens during a restore. The trigger reads the same `app_setting` row the panel writes, so the refusal holds for a `psql` session as well as for the application. **What answering it buys:** "not health data", or "health data but this hosting is acceptable", is one owner-only setting change with a written justification — no migration and no release. "Health data that must stay in the UAE" is the expensive answer and is the one ADR 0010 built the boundary for: the `clinical` schema relocates and takes `CLINICAL_KEK` with it, and this setting stays false until it has. Note what cannot be undone either way: real data loaded before the answer cannot be un-loaded, which is why the strict reading is the default | open | loading real intake data, `C-CRM-08`'s intake page showing anything but fixtures |
| Y1-entity | Mainland, DIFC, ADGM or free zone? Decides which privacy law applies | **Federal PDPL assumed** | open | privacy layer specifics |
| Y11-tax-agent | FTA-registered tax agent review of the VAT working papers. **Not optional** | — | open | first VAT return |
| Y11-vat-package | VAT date of supply on a prepaid package: at sale or at redemption? | **At redemption; deferred-revenue liability on sale** | open | `M-TILL` revenue recognition |
| Y11-vat-invoice | Confirm mandatory tax-invoice field list and the Arabic-language requirement | **Superset of known requirements carried on every invoice** | open | first invoice issued |
| Y1-rooms | Permitted room types and treatment-room count under the licence | **From the handover pack; couples room assumed permitted** | open | `B-CAT` |
| Y12-video-patents | **Do AVC and HEVC patent royalties apply to encoding and self-hosting a short decorative hero loop?** A separate question from copyright: the worker image's ffmpeg is GPL because of libx264 and libx265, and neither licence says anything about the AVC pool (Via LA) or the HEVC pools (Access Advance, Via LA, plus holders outside both). The build cannot answer it and must not assume it away | **The strict position.** The pipeline exists and nothing has been published: no real footage has been encoded, and the only master is the marked stand-in (`Y12-hero-video`). The obligation and the reasoning are written out in `build/container-policy.json` and `apps/worker/licences/THIRD-PARTY-NOTICES.md` so the question is visible to whoever answers it rather than buried in an argv | open | publishing a real hero video |
| Y12-licence-contact | **Which address receives GPL/LGPL source requests?** The worker image conveys ffmpeg, libx264 and libx265 (GPL-2.0-or-later) and libvips (LGPL-3.0-or-later), and each carries a written offer to supply complete corresponding source. An offer needs a working address | **Deliberately blank.** The notice in the image names this id instead of an address, because a plausible address that reaches nobody is worse than a visibly unanswered one — it looks configured. The rest of the offer is complete: the texts are asserted present in the image and the exact installed versions are recorded in it at build time | open | conveying the worker image to anyone outside the business |

## C. External credentials and access — all stubbed, none blocking

| ID | Item | Stub behaviour | Status | Blocks |
|---|---|---|---|---|
| Y2-listing-owner | Which Google account owns the GBP listing, at what role | Fake OAuth + fake account/location picker | open | real `G-CONN` |
| Y3-gbp-api | GBP Basic API Access application | Fake GBP returning `access_not_granted` on demand | open | autoresponder API mode |
| Y4-token-test | The 9-day refresh-token expiry experiment. **Hard blocker — see [10](10-google-connection.md) §6** | Fake OAuth simulates 7-day expiry and `invalid_grant` | open | OAuth publishing decision |
| Y10-consent | Owner completes OAuth consent and confirms the location picker | Fake consent flow, fully testable | open | real `G-CONN` |
| Y6-sender-ids | Two SMSala sender IDs, transactional + `AD-` promotional | Fake SMSala with segment/cost calc, DLRs, suspension simulation | open | real campaigns |
| Y7-mcc | Merchant category code confirmed in writing, two providers | Manual/cash adapter is real; fake card gateway with 3DS and chargebacks | open | `Y-PAY` going live |
| Y13-pentest | Penetration test booked | — | open | go-live |
| Y6-sms-rate | **What does SMSala actually charge per segment, and is the unicode rate quoted separately?** The campaign budget screen multiplies it by a segment count, and Arabic is 70 characters per segment against English's 160 — so the rate decides whether an Arabic campaign is affordable. Two questions rather than one: most aggregators that publish a rate card quote unicode above GSM-7, which would multiply an Arabic campaign twice over | 12 fils per GSM-7 segment and 30 per UCS-2 one, in `SMS_SEGMENT_PRICES` (`@berelax/core`) — the one table the authoring preview, the fake vendor and the cost report all read, carrying `provisionalUntil: 'Y6-sms-rate'` so a screen showing a total can say the rate is unconfirmed. If the contract turns out to charge one rate for both, the two entries become the same number and nothing else changes | open | campaign cost estimates |
| Y7-gateway | **Which card gateway, and is an MCC agreed in writing?** Some acquirers decline this merchant category | Fake gateway with 3DS, partial refund, webhook replay and disputes | open | `Y-PAY` |
| Y6-email-sender | **Which verified Resend sending addresses?** [05](05-external-dependencies.md) §2 asks for separate sending subdomains for transactional and marketing, so a campaign complaint cannot kill booking confirmations — which means two addresses, neither of which the build may guess | The email transport takes its from-address as a required argument with no default, and the fixture supplies one carrying a placeholder marker (`not configured`) on a `.invalid` domain, so nothing can mistake it for a verified sender | open | any real email send |
| Y8-llm-budget | **What monthly token budget does the owner agree to?** The setting exists and is bounded; the number is not the build's to choose | `agents.monthly_token_budget` provisional, and the fake accounts for tokens so the guard is testable | open | `G-SEO` live mode |

## D. Data and assets from the owner

| ID | Item | Provisional | Status | Blocks |
|---|---|---|---|---|
| Y8-menu | Real service menu: names, durations, prices, which are online-bookable | Synthetic 12-service fixture | open | `B-CAT` seeding |
| Y8-rooms | Room inventory: count, types, couples-capable, service compatibility | Synthetic 5-room fixture | open | `B-CAT` seeding |
| Y8-staff | Staff list for the **19 therapists**: names, skills (Asian/Arabic style), languages, gender, certification expiries | **Seeded by `pnpm seed` (P-HR-01) as the headcount and nothing else**: 19 employment records, `display_name` NULL and `photo_consent` false so every one is unpublishable, style skills split 10 Asian / 9 Arabic and flagged provisional. **No gender and no language is invented** — 0030 refused to have a migration "invent nineteen people's genders" and gender is a hard constraint on assignment (B-AVAIL-05), so a made-up one would decide who may treat whom. No credentials either, so none of the nineteen is bookable: availability offering nothing until the HR file exists is the loud failure | open | `P-HR` credentials, therapist pages |
| Y8-hours | Opening hours including Ramadan variation | Synthetic hours | open | `B-AVAIL` |
| Y8-packages | **Outstanding packages already sold** — holder, price, sessions remaining, validity. Reconciled to cash received and signed off | Synthetic balances | open | `H-MIG` |
| Y8-leave | Current leave balance per employee | **Nothing is imported, and the absence is reported rather than defaulted.** `importLeaveOpeningBalances` (P-HR-08) loads one stated figure per employee from the HR handover file as a `leave_movement` of kind `opening_balance`, carrying its source, who loaded it and the date it is stated at; `readLeaveOpeningBalances` answers an employee with no import as a ZERO flagged provisional against this id, never as a bare zero, because an imported zero needs nothing and an un-imported zero needs the file loading and the two are indistinguishable as numbers. Accrual then runs on top of whatever was imported, bounded 24 months back, so history before that window has to come from the import rather than from a hundred derived months | open | `P-HR` accrual opening, `P-HR-08` |
| Y8-opening-balances | Accounting opening balances at a clean period boundary | Zero | open | `M-VAT` |
| Y8-coa | Existing chart of accounts and what the accountant expects monthly | Standard spa CoA | open | `M-VAT` |
| Y8-customers | Customer list. **Imports with `marketing_consent = false` without exception** — see [11](11-execution-plan.md) §7 | Synthetic customers | open | `H-MIG` |
| Y12-photos | **Photo library audit and per-face focal points.** The 19 portraits from the prototype are now in `assets/media/`, and their native ratios span **0.461 to 0.799** — nearly two to one. They are full-length shots with the face in roughly the top fifth, so a 4:5 centre crop produces a row of torsos. **W-SYS-05 measured a second problem in the same library:** the dominant colour of all 12 rendered fixture images falls outside the placeholder band docs/08 §6 declares — lightness 0.134 to 0.783 against a 0.86 floor — so every placeholder is the clamp output rather than the photographs colour, and docs/08 §8s 'pastel photography is a performance asset' does not describe these files | Every portrait carries a focal point of 50%/16%, a defensible default from the framing rather than a per-face measurement. `pnpm media` fails if one is missing | open | `W-SITE` therapist grid |
| Y12-consent-photo | Staff photography consent on record for anyone identifiable. **19 identifiable photographs are now in the repository**, taken from the business's own public site | Therapist cards render the photograph with no name and no link, which is also the launch state: the prototype has 19 photographs and 0 names | open | `W-SITE` therapist pages |
| Y12-hero-video | **The hero loop itself.** docs/08 §6's shot list opens with "hero loop" and there is **no video anywhere in this repository** — the 25 committed assets are 19 portraits, 4 interiors and 2 wordmarks. W-SYS-06 built the four-rendition pipeline with nothing real to encode. Also open inside it: the **HEVC CRF**, because docs/08 §6 states `-crf 26`/`-crf 28` for H.264 and states no number for HEVC, and x265's CRF scale is not x264's | A **marked stand-in master**, not footage: `standInMasterY4m()` renders the real committed `photos/hero-team.jpg` as a `scale(1.0 → 1.06)` ramp — which is docs/08 §8's own cut-order option #6 — into an uncompressed y4m whose header carries `BERELAX-STAND-IN-NOT-REAL-FOOTAGE-Y12-hero-video`. `describeMaster()` reads the marker back, the job's log line prints "from a STAND-IN master", and the build result carries `standIn: true`, so no run can report footage it did not have. HEVC provisionally reuses its H.264 sibling's CRF, which on x265's scale yields a smaller, softer file — the safe direction against the docs/08 §8 byte budget, and `pnpm budgets` is what will notice when real footage makes it wrong | open | `W-SYS-07` attach island, `W-SITE-04` home hero |

## D2. Newly opened by the prototype and live-site review

| ID | Question | Provisional value | Status | Blocks |
|---|---|---|---|---|
| Y2-gbp-status | **Is the Google Business Profile claimed and verified?** GBP API access needs it verified and active **60+ days**, so if it is unclaimed this is now the longest-lead item in the plan | Fake GBP; autoresponder in draft mode | open | real `G-CONN`, autoresponder API mode |
| Y1-nap | **Which WhatsApp number is canonical?** The prototype says `052 510 8633`, the live site says `+971 52 823 9069` | **Neither.** `premises.phone_whatsapp` holds `WHATSAPP-PENDING-Y1-NAP`, which `is_placeholder_text()` refuses; both candidates are recorded in `WHATSAPP_CANDIDATES` with their source and neither is ranked. B-CAT-06 reversed the earlier "use the prototype number": promoting one would publish a number that may not reach the business and would be indistinguishable from one the owner had confirmed. The landline and mobile, which both sources agree on, **are** seeded | open | `premises` seed, all schema and citations |
| Y1-trn | TRN and trade-licence number for tax invoices | Placeholder that fails invoice validation until set | open | issuing any real invoice |
| Y8-rooms | Room count, types, which is the wet room, which are capacity-2 | 5 rooms: 1 wet, 1 capacity-2 couples, 3 standard at capacity 1 — the 0012 stub, seeded unchanged by B-CAT-06. **No two-place standard room was invented**: docs/13 §8 lists the room count among the unknowns, and §4 states Four Hands as two therapists over **one client**, so `rooms.capacity` is correctly 1. Four Hands is unbookable only because 0024 counts appointment *rows* against that column; moving the count onto a per-booking places figure is **B-AVAIL-06**'s | open | `B-AVAIL-06` (capacity counting), availability |
| Y9-poa-prices | Prices for Four Hands, Couple Massage, Full Body Shaving | **No figure at all.** B-CAT-06 reversed the 1.8× derivation: a derived price would be quoted, taken at the till and printed on a tax invoice with nothing marking it as a guess, and ADR 0021 leaves no pricing axis to store a per-shape figure on. The three are rows in `price_on_request` (0032) — **a table with no price column** — carrying the resource requirement docs/13 §4 states, `is_provisional` pinned true, and this id. Answering it deletes the row and adds ordinary catalogue data | open | quoting these three at the desk |
| Y9-shaving-room | Does Full Body Shaving need a specific room or equipment? | Any standard room | open | `service_room_type_compat` |
| Y12-names | **The 19 therapist display names.** The prototype publishes none, and the build does not invent them — a therapist page is unpublishable without one (ADR 0020), so every therapist in the fixture is currently unpublished | Cards read "Name not yet published"; the photograph shows, the page does not link | open | `W-SITE` therapist pages, therapist SEO |
| Y12-separator | **What contrast must a non-semantic separator hold, and on which surfaces?** `--hairline` and `--border` state no minimum, so `scripts/palette.py` derives them against the ground alone. In dark mode `--hairline` `#2A2621` measures **1.01:1** against `--surface-raised` `#292520` — not a faint rule but no rule at all — and `--border` reaches 1.25:1; in light mode `--border` falls to 1.76:1 on `--surface-sand`. Every token that *states* a minimum is now derived against the worst surface text may sit on, and these two were deliberately left out of that change: darkening a separator no rule asks to be darker is a design decision, not a correctness fix | **Unchanged** — still derived against the ground at their 1.5 floor, with the shortfall measured and printed by `pnpm palette` rather than hidden: the "Worst text surface" column in docs/08 §3 states it for every token, separators included | open | `W-SYS` card and panel chrome, any divider inside a raised surface |
| Y12-body-face | **Keep Jost for body text, or adopt a higher-x-height workhorse sans?** Jost is a legibility risk at 17px and worse in dense admin tables | Workhorse sans for body and admin; Jost retained for marketing display | open | `F11`, `W-SYS` |
| Y5-analytics-basis | Lawful basis for the **internal** first-party analytics store, which shares nothing with third parties — a different question from the consent-gated GA4/Meta push | Internal store treated as consent-gated too, i.e. the stricter position | open | `A-FIRST` go-live |
| Y12-ref-loop | **Will the front desk paste the WhatsApp ref code at booking?** If not, attribution honestly stops at the click | Ref field present; capture rate reported rather than assumed | open | funnel completeness |
| Y1-woo-baseline | Crawl and rank baseline of `berelaxmassage.com` before anything changes, for the 301 map | Not yet captured | open | `W-SITE` 301 map, relaunch safety |
| Y1-profiles | **The URLs of the business's own profiles elsewhere: TripAdvisor, Instagram, Facebook.** docs/13 §6 records that a TripAdvisor listing exists and gives no URL, and neither web property publishes a social link | **None published.** `Organization.sameAs` in the JSON-LD carries the site origin alone (W-SITE-03). A plausible URL would bind this entity to somebody else's listing, which is precisely the collision with the airport-spa chain that `sameAs` exists to prevent; `SAME_AS_UNANSWERED` in `@berelax/core` names each absent profile with this id. The GBP URL is `Y2-gbp-status`'s | open | `Organization.sameAs`, entity disambiguation, citation consistency |

---

## E. Owner verification tasks — at deploy-and-check

| ID | Task | Why it cannot be automated |
|---|---|---|
| Y12-pilot | Staff pilot; report what the front desk complains about | Requires real staff on a real floor |
| Y14-devices | Real-device testing: SMS arrival on a UAE handset, OTP autofill on real iOS, receipt printer | I can test logic, not a specific device |

---

## Resolved

| ID | Question | Answer | Date |
|---|---|---|---|
| Y9-style | Is Asian/Arabic a treatment style or a therapist attribute? | **Treatment style.** Service = `(style × treatment)`; style maps to a required therapist skill for eligibility only. Pricing and therapist assignment stay decoupled | 2026-09-18 |
| Y12-names | Therapist naming — full, first-name-only, or anonymous? | **Names set in the backend by the admin.** Build the fields; a therapist page publishes only with a display name *and* recorded photography consent | 2026-09-18 |
| Y12-design | Is the Netlify prototype the design direction or a placeholder? | **A prototype to evolve.** Palette and typefaces inherited and made accessible; single-page anchors evolve into real indexable routes | 2026-09-18 |
| Y8-menu | Real service menu with durations and prices | **Extracted** — 8 services × 4 durations, 32 AED gross prices. See [13-business-profile.md](13-business-profile.md) §4 | 2026-09-18 |
| Y8-hours | Opening hours | **Daily 11:00–02:00.** Crosses midnight; `business_day` is first-class | 2026-09-18 |
