{
  "about": "Every secret this system holds, what it controls, where it lives and how it is rotated. Read by scripts/check-secret-rotation.mjs, which refuses a secret-shaped environment variable the code reads and this file does not classify, an entry whose rotation procedure is not a real heading in docs/runbooks/key-rotation.md, and a key-encrypting key with no retired slot to keep the outgoing version in. It is the companion to build/secret-allowlist.json: that file answers 'is a credential committed', this one answers 'when this one leaks, what does somebody do about it at 02:00'. No value is ever stored here \u2014 only names, owners and procedures.",
  "notSecrets": [
    {
      "name": "TEST_DATABASE_URL",
      "reason": "Non-production only, and the credential in it is the documented loopback development one. scripts/check-secrets.mjs exempts loopback hosts structurally for the same reason: a database nobody outside the machine can reach is not a secret. A value here pointing anywhere but loopback is a misconfiguration, not a rotation."
    }
  ],
  "entries": [
    {
      "id": "clinical-kek",
      "kind": "kek",
      "env": "CLINICAL_KEK",
      "versionEnv": "CLINICAL_KEK_VERSION",
      "retiredEnv": "CLINICAL_KEK_PREVIOUS",
      "retiredVersionEnv": "CLINICAL_KEK_PREVIOUS_VERSION",
      "stored": "The deployment secret store, injected as an environment variable. Never in the database: clinical.kek_version holds version labels only.",
      "holds": "Every per-record clinical data key, and therefore every intake submission and treatment note. Holding it plus a database dump is holding the health data; holding either alone is not.",
      "rotation": "docs/runbooks/key-rotation.md#rotating-the-clinical-kek",
      "rotatePeriodDays": 365,
      "zeroDowntime": true,
      "owner": "whoever holds the deployment secret store",
      "notes": "Rotation re-wraps data keys and rewrites no ciphertext, so it runs against a live database. Exercised against synthetic fixtures only until OPEN-QUESTIONS Y5-residency is answered and real intake data may be loaded."
    },
    {
      "id": "google-token-kek",
      "kind": "kek",
      "env": "GOOGLE_TOKEN_KEK",
      "versionEnv": "GOOGLE_TOKEN_KEK_VERSION",
      "retiredEnv": "GOOGLE_TOKEN_KEK_PREVIOUS",
      "retiredVersionEnv": "GOOGLE_TOKEN_KEK_PREVIOUS_VERSION",
      "stored": "The deployment secret store, injected as an environment variable. public.google_connection.refresh_token_kid holds the version label only.",
      "holds": "The wrapped data key of every Google refresh token. With a database dump it is control of the business Google presence, and the scope has no read-only variant: the token that reads reviews also rewrites the address and the opening hours (docs/10 section 3).",
      "rotation": "docs/runbooks/key-rotation.md#rotating-the-google-token-kek",
      "rotatePeriodDays": 365,
      "zeroDowntime": true,
      "owner": "whoever holds the deployment secret store",
      "notes": "A separate key from the clinical one deliberately. packages/google/src/rewrap.ts performs the re-wrap and has no caller yet, so this rotation is procedural rather than scripted \u2014 see the NOTE on H-HARD-03 in build/manifest.yaml."
    },
    {
      "id": "staff-pii-kek",
      "kind": "kek",
      "env": "STAFF_PII_KEK",
      "versionEnv": "STAFF_PII_KEK_VERSION",
      "retiredEnv": "STAFF_PII_KEK_PREVIOUS",
      "retiredVersionEnv": "STAFF_PII_KEK_PREVIOUS_VERSION",
      "stored": "The deployment secret store, injected as an environment variable. Never in the database: employee_bank_detail.detail_kid and employee_document.number_kid hold version labels only, and there is deliberately no version registry table yet (migration 0050 says why).",
      "holds": "The wrapped data key of every staff bank account and every identity-document number - the IBANs a salary file pays into and the Emirates ID and passport numbers of nineteen identifiable people. With a database dump it is those numbers; either alone is not.",
      "rotation": "docs/runbooks/key-rotation.md#rotating-the-staff-pii-kek",
      "rotatePeriodDays": 365,
      "zeroDowntime": true,
      "owner": "whoever holds the deployment secret store",
      "notes": "A third key, separate from the clinical one deliberately: an employment record does not relocate with the clinical store (ADR 0010), so one key would either strand this estate at relocation or exist in two places. packages/hr/src/staff-secret.ts performs the per-row re-wrap and has no caller, so this rotation is a runbook procedure rather than a command - the same position packages/google/src/rewrap.ts is in, and for the same reason recorded there."
    },
    {
      "id": "database-url",
      "kind": "database-credential",
      "env": "DATABASE_URL",
      "stored": "The deployment secret store. The password half is also in the DigitalOcean control panel, which is where it is changed.",
      "holds": "Every row in the database, including the ciphertext of the clinical payloads and of the Google refresh tokens. It does not hold either key, which is the whole point of the envelope.",
      "rotation": "docs/runbooks/key-rotation.md#rotating-the-database-credential",
      "rotatePeriodDays": 365,
      "zeroDowntime": false,
      "outage": "A managed-database password change is immediate and drops every open connection; the application reconnects with the new URL only after it is redeployed, so there is a gap of one deploy. Do it outside trading hours (11:00 to 02:00, Asia/Dubai).",
      "owner": "whoever holds the DigitalOcean account"
    },
    {
      "id": "payload-secret",
      "kind": "signing-key",
      "env": "PAYLOAD_SECRET",
      "stored": "The deployment secret store.",
      "holds": "Signs Payload admin session tokens and password-reset links. Holding it is the ability to mint an admin session without a password, which bypasses the authorisation matrix and mandatory TOTP (ADR 0009).",
      "rotation": "docs/runbooks/key-rotation.md#rotating-the-payload-secret",
      "rotatePeriodDays": 180,
      "zeroDowntime": false,
      "outage": "Every admin session and every unused password-reset link is invalidated the moment it changes. Nobody is locked out permanently, but everybody signs in again, so it is announced rather than done quietly.",
      "owner": "whoever holds the deployment secret store"
    },
    {
      "id": "google-oauth-client-secret",
      "kind": "oauth-client-secret",
      "env": "GOOGLE_OAUTH_CLIENT_SECRET",
      "stored": "The deployment secret store, and the Google Cloud console, which is where it is rotated.",
      "holds": "With a refresh token it mints access tokens for the business Google presence. A separate client per environment, because re-consenting against the production client id silently invalidates the oldest refresh tokens (docs/10 section 4).",
      "rotation": "docs/runbooks/key-rotation.md#rotating-the-google-oauth-client-secret",
      "rotatePeriodDays": 365,
      "zeroDowntime": true,
      "owner": "whoever holds the business Google account",
      "notes": "Google allows two client secrets on one client at once, so the new one is added, deployed, and the old one deleted afterwards. Stored refresh tokens survive: they are bound to the client id, not the secret."
    },
    {
      "id": "sentry-dsn",
      "kind": "telemetry-key",
      "env": "SENTRY_DSN",
      "stored": "The deployment secret store.",
      "holds": "Write access to the error-reporting project. A leak means somebody can flood it, which matters because a flooded project is one nobody reads. It grants no read access to events already reported.",
      "rotation": "docs/runbooks/key-rotation.md#rotating-the-sentry-dsn",
      "rotatePeriodDays": 730,
      "zeroDowntime": true,
      "owner": "whoever holds the Sentry account"
    },
    {
      "id": "google-refresh-token",
      "kind": "stored-credential",
      "env": null,
      "stored": "public.google_connection.refresh_token_ct, sealed under GOOGLE_TOKEN_KEK. Never in an environment variable and never in a log; scripts/check-google-token-chokepoint.mjs enforces where it may be named at all.",
      "holds": "Durable control of the business Google presence: reviews, the business profile, the address and the opening hours. It does not expire on its own.",
      "rotation": "docs/runbooks/key-rotation.md#rotating-a-google-refresh-token",
      "rotatePeriodDays": 365,
      "zeroDowntime": false,
      "outage": "Rotating the token itself needs the owner to re-consent in a browser, and the connection reports needs_reauth until they do. Rotating the KEK that wraps it does not \u2014 that is a re-wrap, and it is the reason refresh_token_kid exists.",
      "owner": "the business Google account owner"
    },
    {
      "id": "suppression-pepper",
      "kind": "signing-key",
      "env": "SUPPRESSION_PEPPER",
      "versionEnv": "SUPPRESSION_PEPPER_VERSION",
      "retiredEnv": "SUPPRESSION_PEPPER_PREVIOUS",
      "retiredVersionEnv": "SUPPRESSION_PEPPER_PREVIOUS_VERSION",
      "stored": "The deployment secret store, injected as an environment variable. Never in the database: suppression.pepper_version holds a version label only, and a pepper stored beside the digests it peppers adds nothing against the one attack it exists to answer - a database dump.",
      "holds": "The HMAC key behind suppression.key_hmac (migration 0064). With a database dump it is the list of every phone number and address that has opted out of marketing, because the UAE mobile space is about ten million numbers per prefix and an unpeppered digest of one is enumerable on a laptop. Either alone is not: the table holds no plaintext, and the pepper decrypts nothing.",
      "rotation": "docs/runbooks/key-rotation.md#rotating-the-suppression-pepper",
      "rotatePeriodDays": 730,
      "zeroDowntime": true,
      "owner": "whoever holds the deployment secret store",
      "notes": "Classified signing-key and not kek: nothing is encrypted with it, so no record becomes unreadable if it is lost. The retired slot is declared anyway and is the important half - reads consult both peppers, so the rotation itself is seamless, but a row whose plaintext recipient this system no longer holds can never be re-keyed (customer has no email column at all, C-CRM-01 NOTE 3), so SUPPRESSION_PEPPER_PREVIOUS is retained indefinitely rather than until a sweep finishes. Deleting it turns those rows into rows nothing will ever match, which presents as a promotional message to somebody who opted out. The runbook states this under its own heading."
    }
  ]
}
